# 🤖 Trae AI Builder Development Workflow
**Visual Guide for Claude Sonnet 4 Development Process**

---

## 📋 Complete Feature Development Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   STEP 1: PLANNING                          │
│                                                             │
│  ✓ Understand feature requirements                         │
│  ✓ Review relevant documentation                           │
│  ✓ Identify required permissions                           │
│  ✓ Plan database structure (KV Store keys)                 │
│  ✓ List components needed                                  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 2: DATABASE SETUP                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ Test Connection:                                    │  │
│  │ → Navigate to /backend-dashboard                    │  │
│  │ → Click "Test Database Connection"                  │  │
│  │ → Verify ✅ Connection Successful                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ Plan KV Store Keys:                                 │  │
│  │ → Choose prefix (e.g., "booking:")                  │  │
│  │ → Define data structure                             │  │
│  │ → Plan relationships                                │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 3: BACKEND DEVELOPMENT                    │
│                                                             │
│  File: /supabase/functions/server/index.tsx                │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ import * as kv from './kv_store.tsx';               │  │
│  │                                                      │  │
│  │ app.post('/make-server-84a71643/endpoint', ...)     │  │
│  │ app.get('/make-server-84a71643/endpoint', ...)      │  │
│  │ app.put('/make-server-84a71643/endpoint/:id', ...)  │  │
│  │ app.delete('/make-server-84a71643/endpoint/:id',..) │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ✓ CRUD operations with KV store                           │
│  ✓ Error handling (try/catch)                              │
│  ✓ Logging (console.log)                                   │
│  ✓ Input validation                                        │
│  ✓ Auth middleware (if needed)                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 4: FRONTEND COMPONENTS                    │
│                                                             │
│  A. Create Page Component                                  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ File: /pages/MyPage.tsx                             │  │
│  │                                                      │  │
│  │ import { AdminLayout } from '@/components/layout'   │  │
│  │ import { useTheme } from '@/components/layout'      │  │
│  │                                                      │  │
│  │ const MyPage = () => {                              │  │
│  │   const { theme } = useTheme();                     │  │
│  │   const isDark = theme === 'dark';                  │  │
│  │                                                      │  │
│  │   return (                                           │  │
│  │     <AdminLayout>                                    │  │
│  │       <PageHeader title="..." />                    │  │
│  │       {/* Content */}                                │  │
│  │     </AdminLayout>                                   │  │
│  │   );                                                 │  │
│  │ };                                                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  B. Create Child Components                                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ • Data tables                                        │  │
│  │ • Form dialogs                                       │  │
│  │ • Detail views                                       │  │
│  │ • Action buttons                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ✓ Dark mode support (ThemeContext)                        │
│  ✓ Mobile-responsive (grid, flex)                          │
│  ✓ Permission guards                                       │
│  ✓ Loading states                                          │
│  ✓ Error states                                            │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 5: API INTEGRATION                        │
│                                                             │
│  File: /utils/api/myFeature.ts (optional)                  │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ import { projectId, publicAnonKey } from '...'      │  │
│  │                                                      │  │
│  │ export const myApi = {                              │  │
│  │   getAll: async () => { ... },                      │  │
│  │   create: async (data) => { ... },                  │  │
│  │   update: async (id, data) => { ... },              │  │
│  │   delete: async (id) => { ... },                    │  │
│  │ };                                                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  Or inline in component:                                   │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ const fetchData = async () => {                     │  │
│  │   const response = await fetch(url, {               │  │
│  │     headers: {                                       │  │
│  │       'Authorization': `Bearer ${publicAnonKey}`    │  │
│  │     }                                                 │  │
│  │   });                                                │  │
│  │   const data = await response.json();               │  │
│  │ };                                                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ✓ Error handling                                          │
│  ✓ Loading states                                          │
│  ✓ Toast notifications (success/error)                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 6: STYLING & RESPONSIVE                   │
│                                                             │
│  A. Dark Mode                                              │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ const bgClass = isDark ? 'bg-[#161616]' : 'bg-white'│  │
│  │ const textClass = isDark ? 'text-white' : '...'     │  │
│  │ const borderClass = isDark ? 'border-gray-800':'...'│  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  B. Explicit Styling                                       │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ <Input className="h-12 bg-gray-100 border-gray-300" │  │
│  │        placeholder:text-gray-500" />                │  │
│  │                                                      │  │
│  │ <Label className="text-gray-700">Name</Label>       │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  C. Mobile-First Responsive                                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ <div className="grid grid-cols-1 md:grid-cols-2    │  │
│  │               lg:grid-cols-3">                       │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 7: PERMISSIONS & SECURITY                 │
│                                                             │
│  A. Add Permission Guards                                  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ import { PermissionGuard } from '...'               │  │
│  │                                                      │  │
│  │ <PermissionGuard permissions={['bookings.edit']}>  │  │
│  │   <Button>Edit</Button>                             │  │
│  │ </PermissionGuard>                                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  B. Protected API Routes (Backend)                         │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ const token = c.req.header('Authorization')...      │  │
│  │ const { user } = await supabase.auth.getUser(token) │  │
│  │ if (!user) return c.json({ error: '...' }, 401);    │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ✓ Never expose SUPABASE_SERVICE_ROLE_KEY in frontend     │
│  ✓ Use publicAnonKey for frontend requests                │
│  ✓ Validate user permissions                               │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 8: TESTING                                │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ □ Test dark mode toggle                             │  │
│  │ □ Test mobile view (375px)                          │  │
│  │ □ Test tablet view (768px)                          │  │
│  │ □ Test desktop view (1024px+)                       │  │
│  │ □ Test all CRUD operations                          │  │
│  │ □ Test error scenarios                              │  │
│  │ □ Test loading states                               │  │
│  │ □ Test permission guards                            │  │
│  │ □ Check browser console (no errors)                 │  │
│  │ □ Test keyboard navigation                          │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              STEP 9: DOCUMENTATION                          │
│                                                             │
│  ✓ Add comments to complex logic                           │
│  ✓ Update route in App.tsx                                 │
│  ✓ Update sidebar navigation (if needed)                   │
│  ✓ Document API endpoints                                  │
│  ✓ Update permissions list (if new ones added)             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              ✅ DEPLOYMENT READY                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Decision Trees

### When to Use What?

#### State Management
```
Need to store data?
│
├─ Temporary (session only)?
│  └─ ✓ Use React useState/useContext
│
├─ Persistent (across sessions)?
│  ├─ Simple key-value?
│  │  └─ ✓ Use KV Store
│  │
│  └─ Complex queries (JOINs, full-text search)?
│     └─ ⚠️ Consider custom table (ask user to create in Supabase UI)
```

#### API Structure
```
Building API endpoint?
│
├─ Public (no auth required)?
│  └─ ✓ Use publicAnonKey in frontend
│
├─ Protected (auth required)?
│  ├─ User-specific data?
│  │  └─ ✓ Verify token, filter by user.id
│  │
│  └─ Admin-only?
│     └─ ✓ Verify token + check user.role
```

#### Component Organization
```
Creating UI?
│
├─ Full page?
│  └─ ✓ Create in /pages/
│     └─ Wrap with <AdminLayout>
│
├─ Reusable component?
│  ├─ General purpose?
│  │  └─ ✓ Create in /components/ui/
│  │
│  └─ Feature-specific?
│     └─ ✓ Create in /components/[feature]/
```

---

## 🔄 Iteration Workflow

```
┌─────────────────────────────────────────────────┐
│              BUILD ITERATION                    │
└────────────┬────────────────────────────────────┘
             │
             ├─> 1. Build feature
             │   └─> Write code
             │
             ├─> 2. Check dark mode
             │   └─> Toggle theme, verify colors
             │
             ├─> 3. Test mobile
             │   └─> Resize browser, check layout
             │
             ├─> 4. Test functionality
             │   └─> Try all actions (CRUD)
             │
             ├─> 5. Check errors
             │   └─> Open console, look for red
             │
             └─> 6. Fix & repeat
                 └─> Address issues, go to step 1

When all tests pass → Move to next feature
```

---

## 📊 Database Workflow

### KV Store Pattern Flow

```
┌───────────────────────────────────────────────────┐
│           PLANNING PHASE                          │
│                                                   │
│  1. Define entity (e.g., "Booking")               │
│  2. Choose prefix (e.g., "booking:")              │
│  3. Design data structure                         │
│     {                                              │
│       id: string,                                  │
│       customerName: string,                        │
│       gameId: string,                              │
│       date: string,                                │
│       status: 'pending' | 'confirmed' | ...        │
│     }                                              │
└────────────┬──────────────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────────────────┐
│           CREATE OPERATION                        │
│                                                   │
│  Backend:                                         │
│  const id = crypto.randomUUID();                  │
│  await kv.set(`booking:${id}`, {                  │
│    id,                                             │
│    ...data,                                        │
│    createdAt: new Date().toISOString(),           │
│  });                                               │
│                                                   │
│  Frontend:                                        │
│  POST /make-server-84a71643/bookings              │
│  Body: { customerName: '...', ... }               │
└────────────┬──────────────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────────────────┐
│           READ OPERATIONS                         │
│                                                   │
│  Single:                                          │
│  const booking = await kv.get('booking:123');     │
│  GET /make-server-84a71643/bookings/123           │
│                                                   │
│  Multiple:                                        │
│  const bookings = await kv.getByPrefix('booking:');│
│  GET /make-server-84a71643/bookings               │
│                                                   │
│  Filter in code:                                  │
│  const pending = bookings.filter(b =>             │
│    b.status === 'pending'                         │
│  );                                                │
└────────────┬──────────────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────────────────┐
│           UPDATE OPERATION                        │
│                                                   │
│  Backend:                                         │
│  const existing = await kv.get(`booking:${id}`);  │
│  await kv.set(`booking:${id}`, {                  │
│    ...existing,                                    │
│    ...updates,                                     │
│    updatedAt: new Date().toISOString(),           │
│  });                                               │
│                                                   │
│  Frontend:                                        │
│  PUT /make-server-84a71643/bookings/123           │
│  Body: { status: 'confirmed' }                    │
└────────────┬──────────────────────────────────────┘
             │
             ▼
┌───────────────────────────────────────────────────┐
│           DELETE OPERATION                        │
│                                                   │
│  Backend:                                         │
│  await kv.del(`booking:${id}`);                   │
│                                                   │
│  Frontend:                                        │
│  DELETE /make-server-84a71643/bookings/123        │
└───────────────────────────────────────────────────┘
```

---

## 🎨 UI Development Workflow

```
┌─────────────────────────────────────────────┐
│         COMPONENT CREATION                  │
└────┬────────────────────────────────────────┘
     │
     ├─> 1. Import ThemeContext
     │   import { useTheme } from '@/components/layout/ThemeContext'
     │
     ├─> 2. Get theme state
     │   const { theme } = useTheme();
     │   const isDark = theme === 'dark';
     │
     ├─> 3. Define semantic variables
     │   const bgClass = isDark ? 'bg-[#161616]' : 'bg-white';
     │   const textClass = isDark ? 'text-white' : 'text-gray-900';
     │
     ├─> 4. Apply to elements
     │   <div className={`${bgClass} ${textClass}`}>
     │
     ├─> 5. Add explicit styling
     │   <Input className="h-12 bg-gray-100 border-gray-300" />
     │
     ├─> 6. Make responsive
     │   <div className="grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
     │
     └─> 7. Test both themes
         Toggle dark mode, verify all elements
```

---

## 🔐 Authentication Workflow

```
┌─────────────────────────────────────────────────┐
│              USER LOGIN                         │
└────┬────────────────────────────────────────────┘
     │
     ├─> Frontend: Login Page
     │   ├─> User enters email/password
     │   ├─> Call supabase.auth.signInWithPassword()
     │   ├─> Receive session with access_token
     │   └─> Store token in localStorage
     │
     ▼
┌─────────────────────────────────────────────────┐
│           MAKING API REQUESTS                   │
└────┬────────────────────────────────────────────┘
     │
     ├─> Get token from localStorage
     │   const token = localStorage.getItem('supabase_access_token');
     │
     ├─> Include in Authorization header
     │   headers: {
     │     'Authorization': `Bearer ${token}`
     │   }
     │
     ▼
┌─────────────────────────────────────────────────┐
│        BACKEND VERIFICATION                     │
└────┬────────────────────────────────────────────┘
     │
     ├─> Extract token from header
     │   const token = c.req.header('Authorization')?.split(' ')[1];
     │
     ├─> Verify with Supabase
     │   const { user } = await supabase.auth.getUser(token);
     │
     ├─> Check user exists
     │   if (!user) return 401 Unauthorized;
     │
     └─> Process request
         return protected data
```

---

## 🚨 Error Handling Workflow

```
┌─────────────────────────────────────────────────┐
│          EVERY ASYNC OPERATION                  │
└────┬────────────────────────────────────────────┘
     │
     ├─> Wrap in try/catch
     │   try {
     │     // Operation
     │   } catch (error) {
     │     // Handle error
     │   }
     │
     ├─> Log errors
     │   console.error('Operation failed:', error);
     │
     ├─> Show user feedback
     │   toast.error('Failed to...');
     │
     └─> Provide recovery option
         <Button onClick={retry}>Try Again</Button>
```

---

## 📱 Responsive Design Workflow

```
┌─────────────────────────────────────────────────┐
│        MOBILE-FIRST APPROACH                    │
└────┬────────────────────────────────────────────┘
     │
     ├─> 1. Design for mobile (375px)
     │   <div className="p-4">
     │   <div className="grid-cols-1">
     │
     ├─> 2. Add tablet breakpoint (768px)
     │   <div className="p-4 md:p-6">
     │   <div className="grid-cols-1 md:grid-cols-2">
     │
     ├─> 3. Add desktop breakpoint (1024px+)
     │   <div className="p-4 md:p-6 lg:p-8">
     │   <div className="grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
     │
     └─> 4. Test at each breakpoint
         Resize browser, verify layout
```

---

## ✅ Quality Checklist Workflow

```
Before considering feature complete:

DESIGN:
├─ [✓] Dark mode works in both themes
├─ [✓] Mobile view (375px) looks good
├─ [✓] Tablet view (768px) looks good
├─ [✓] Desktop view (1024px+) looks good
├─ [✓] All inputs use bg-gray-100 border-gray-300
├─ [✓] All cards use bg-white border-gray-200
└─ [✓] Primary buttons use #4f46e5

FUNCTIONALITY:
├─ [✓] Create operation works
├─ [✓] Read operation works
├─ [✓] Update operation works
├─ [✓] Delete operation works
├─ [✓] Search/filter works (if applicable)
└─ [✓] Pagination works (if applicable)

ERROR HANDLING:
├─ [✓] API errors show toast messages
├─ [✓] Loading states display
├─ [✓] Empty states handled
├─ [✓] Form validation works
└─ [✓] No console errors

SECURITY:
├─ [✓] Permission guards applied
├─ [✓] No service keys in frontend
├─ [✓] Protected routes verified
└─ [✓] User input sanitized

PERFORMANCE:
├─ [✓] No unnecessary re-renders
├─ [✓] Data fetched efficiently
└─ [✓] Images optimized (if any)
```

---

## 🎓 Learning Paths

### Path 1: Complete Beginner (Day 1)
```
Hour 1: Read /TRAE_AI_BUILDER_MASTER_GUIDE.md
Hour 2: Review /pages/Dashboard.tsx
Hour 3: Create simple page with dark mode
Hour 4: Test and refine
```

### Path 2: Database Focus (Day 2)
```
Hour 1: Read database patterns in master guide
Hour 2: Test KV store in Backend Dashboard
Hour 3: Create CRUD API endpoints
Hour 4: Build frontend to consume API
```

### Path 3: Full Feature (Day 3-4)
```
Day 3:
├─ Plan feature structure
├─ Build backend API
├─ Create KV store schema
└─ Test with Backend Dashboard

Day 4:
├─ Build frontend components
├─ Add dark mode support
├─ Make responsive
├─ Add permissions
└─ Test thoroughly
```

---

**Version**: 4.0  
**Last Updated**: November 4, 2025  
**For Complete Guide**: `/TRAE_AI_BUILDER_MASTER_GUIDE.md`

---

**Follow this workflow for consistent, high-quality development! 🚀**
